# Run with `dart run ffigen --config ffigen.yaml`.
name: FlutterMediaToolsBindings
description: |
  Bindings for `src/media_probe.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/flutter_media_tools_bindings_generated.dart'
headers:
  entry-points:
    - 'src/media_probe.h'
  include-directives:
    - 'src/media_probe.h'
ffi-native:
comments:
  style: any
  length: full
